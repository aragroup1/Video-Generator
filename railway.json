{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate && if [ \"$IS_WORKER\" != \"true\" ]; then npm run build; fi"
  },
  "deploy": {
    "startCommand": "if [ \"$IS_WORKER\" = \"true\" ]; then node worker.js; else npx prisma db push && npm start; fi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
